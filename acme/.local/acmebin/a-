#!/bin/sh
# a- - delete (close) the current acme window
# Usage: a-
# Must be run from within an acme window (requires $winid)

if [ -z "$winid" ]; then
    echo "a-: must be run from within an acme window" >&2
    exit 1
fi

echo delete | 9p write acme/$winid/ctl
