#!/bin/sh
# aget - reload the current acme window from disk
# Usage: aget

if [ -z "$winid" ]; then
    echo "aget: must be run from within an acme window" >&2
    exit 1
fi

echo get | 9p write acme/$winid/ctl
