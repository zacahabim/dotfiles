#!/bin/sh
# aput - write (save) the current acme window to disk
# Usage: aput

if [ -z "$winid" ]; then
    echo "aput: must be run from within an acme window" >&2
    exit 1
fi

echo put | 9p write acme/$winid/ctl
